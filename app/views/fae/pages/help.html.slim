= render 'fae/shared/index_header', title: 'Need some help?', breadcrumbs: false, new_button: false

section.help-section
  article.content
    h2 Contact Us
    p
      strong> Phone:
      | 916.776.6249
      br
      strong> Email:
      a href="mailto:support@koreinnovations.io" support@koreinnovations.io
      br
      strong> Website:
      a href="https://www.koreinnovations.io" target="_blank" support@koreinnovations.io

  article.content
    h2 Reporting A Bug?

    h3 Please include this helpful information:
    ruby:
      help_information = {
        'Version'  => "Admin Tools v#{Fae::VERSION}",
        'Date'     => Time.now,
        'Site'     => request.url,
        'Env'      => Rails.env,
        'Browser'  => "#{@browser.name} #{@browser.full_version}",
        'Platform' => @browser.platform,
        'User ID'  => current_user.id
      }

    p
      - help_information.each do |identifier, value|
        strong> #{identifier}:
        = value
        br
